import React from 'react';

//ReactDOM is the package in the react ecosystem that provides methods to interact with the DOM (Document Object Model) in the web 
//applications. It is a bridge between the React components and the actual DOM elements that are rendered in the browser.
import ReactDOM from 'react-dom';

import App from './App';

ReactDOM.render(<App/> , document.getElementById('root'));